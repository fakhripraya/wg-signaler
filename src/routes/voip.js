const voipRoute = (app) => {
  // GET method route
  app.get(`/v1/room/check`, (req, res) => {});

  // POST method route
  app.post(`/v1/room/create`, (req, res) => {});
};

export default voipRoute;
